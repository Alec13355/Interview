const findMissing = require('./index');

describe('tests', () => {
    // test('should return 0 with empty list', () => {
    //     const count = findMissing([]);
    //     expect(count.length).toEqual(0);
    // });

    // test('should return number if called with only 1 in list', () => {
    //     const list = findMissing([1]);
    //     expect(list[0]).toEqual(1);
    //     expect(list.length).toEqual(1);
    // })

    // test('with one mismatch should return it', () => {
    //     const list = findMissing([1,2,1]);
    //     expect(list[0]).toEqual(2);
    //     expect(list.length).toEqual(1);
    // });

    // test('with all mismatch return all', () => {
    //     const list = findMissing([99,34,23]);
    //     expect(list[0]).toEqual(99);
    //     expect(list[1]).toEqual(34);
    //     expect(list[2]).toEqual(23);
    //     expect(list.length).toEqual(3);
    // });

    // test('with all pairs returns an empty list', () => {
    //     const list = findMissing([1,2,3,4,4,2,1,3,4,4]);
    //     expect(list.length).toEqual(0);
    // });
});